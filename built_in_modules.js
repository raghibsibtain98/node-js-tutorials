/*const os = require('os') //we dont need a slash or etc because it is a built in module
console.log(os.userInfo())
console.log(`the system uptime is ${os.uptime()}`)
//console.log(os.type())
//console.log(os.arch())
//console.log(os.cpus());
const currentOS = {
    name: os.type(),
    arch: os.arch(),
    release: os.release(),
    totalMem: os.totalmem(),
    freeMem: os.freemem()
}
console.log(currentOS)*/

/*const path = require('path')
console.log(path.sep) //gives system specific path separator

const filepath = path.join('/content_example/','test.txt')
console.log(filepath);

const basename = path.basename(filepath)
console.log(basename)

const absolute = path.resolve('content_example','test.txt')
console.log(absolute);*/

